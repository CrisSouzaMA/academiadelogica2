import React from 'react';
import { screen } from '@testing-library/react';
import renderWithRouter from './renderWithRouter';
import App from '../App';
import Home from '../Pages/Home';
import Details from '../Components/Details';
import userEvent from '@testing-library/user-event';

describe('Testando a aplicação Academia de Lógica 2', () => {
  it('Testa se tem uma tag h1 com o texto "Academia de Lógica - Parte 2"', () => {
   
  });

  it('Testa se tem uma tag h2 com o texto "Imagem do status"', () => {
    
   });

   it('Testa se os botões aparecem na tela', () => {
    
   });

   it('Testa se ao clicar no botão é redirecionado para outra pagina', () => {
    
   });   

   it('Testa a requisição da API', () => {
    
   });     
   
   it('Teste o valor do campo input e se está sendo guardado', () => {
    
   });   
});